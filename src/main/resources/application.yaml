spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # localhost:3306 뒤의 skhuthon은 1단계에서 만든 데이터베이스 이름
    url: jdbc:mysql://localhost:3306/skhuthon?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
    username: root
    password: wert030415
  jpa:
    hibernate:
      ddl-auto: none

    # JPA가 실행하는 쿼리를 콘솔에서 보기 좋게 출력
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true